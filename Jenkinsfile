pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Update build commands if necessary
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Update test commands if necessary
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Update deployment commands if necessary
            }
        }
    }
    post {
        always {
            echo 'Pipeline completed.'
        }
    }
}
